class AppConstants {
  // Use localhost for local development since the start_all.sh exposes the backend on port 5000.
  // Change to 'http://localhost:5000' for production deployments.
  static const String apiBaseUrl = 'http://localhost:5000';
}
